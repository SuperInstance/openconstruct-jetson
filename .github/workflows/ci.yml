name: CI

on:
  push:
    branches: ['*']
  pull_request:
    branches: [main, master]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install clang-format
        run: sudo apt-get update && sudo apt-get install -y clang-format
      - name: Format check
        run: |
          find src -name '*.cpp' -o -name '*.h' -o -name '*.hpp' | head -20 | while read f; do
            clang-format --dry-run --Werror "$f"
          done

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - name: Configure (CPU mock — no CUDA toolkit on this runner)
        run: cmake -B build -S . -DUSE_CUDA=OFF -DUSE_TENSORRT=OFF -DBUILD_TESTS=ON
      - name: Build
        run: cmake --build build
      - name: Test
        run: cd build && ctest --output-on-failure

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install build tools
        run: |
          sudo apt-get update
          sudo apt-get install -y cmake build-essential
      - name: Configure and build (release, CPU mock — no CUDA toolkit on this runner)
        run: |
          cmake -B build -S . -DCMAKE_BUILD_TYPE=Release -DUSE_CUDA=OFF -DUSE_TENSORRT=OFF
          cmake --build build
